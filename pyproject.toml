[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "qhw-scheduler"
version = "0.1.0"
description = "QPU-local scheduler C ABI with Python bindings"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["python"]

